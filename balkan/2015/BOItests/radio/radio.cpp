#include <bits/stdc++.h>
using namespace std;

const int N = 100010;

int n, k, X[N], P[N], S[N];
vector<int> Zcost, Zx;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	cin >> n >> k;
	assert(n == k);
	for (int i = 1; i <= n; ++i) {
		cin >> X[i] >> P[i] >> S[i];
		
	}
	
}