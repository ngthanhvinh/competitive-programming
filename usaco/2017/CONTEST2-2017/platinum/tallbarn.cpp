#include <iostream>
using namespace std;

int N; long long K;
long long a[100010];

int main() {
    ios_base::sync_with_stdio(false);
    cin >> N >> K;
    for (int i = 1; i <= N; ++i) cin >> a[i];

}
