#include <iostream>
using namespace std;

long long n;

void solve() {
	cin >> n;
	long long l = (n + 2) / 2, r = n;
	for (int i = 1; i <= 60; ++i) {
		
	}
}

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	int t; cin >> t;
	while(t--)
		solve();
}