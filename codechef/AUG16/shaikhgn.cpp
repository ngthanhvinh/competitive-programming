#include <bits/stdc++.h>
using namespace std;

#define ff(i, a, b) for (int i = (a); i <= (b); i++)
#define fod(i, a, b) for (int i = (a); i >= (b); i--)
#define ll long long
#define pb push_back
#define x first
#define y second
typedef pair <int, int> ii;
typedef vector <int> vi;
const int N = 100010, INF = (int)1e9, mod = (int)1e9 + 7;

int main() {
	ios_base::sync_with_stdio(false); cin.tie(0);
	
}