#include "coprobber.h"
#include <bits/stdc++.h>
using namespace std;

int start(int N, bool A[MAX_N][MAX_N]) {
    return 0;
}

int cnt;
int nextMove(int R) {
    ++cnt;
    if (cnt == 1) return 0;
    else return R;
}
