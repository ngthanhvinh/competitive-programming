#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	cin >> n >> m;
	for (int i = 1; i <= m; ++i) {
		cin >> a[i].l >> a[i].r; a[i].id = i;
	}
	sort(a + 1, a + m + 1);

	int maxr = 0;
	for (int i = 1; i <= m; ++i) {
		
	}
}